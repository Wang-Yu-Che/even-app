# 项目约定

- 默认中文沟通；修改保持最小范围，保留工作区已有改动。
- 眼镜画布为 576×288，原生文本行高 27px，不支持字体大小与粗细设置。
- 终端和完成页文案在 `agentview.go` 组合，容器位置在 `evenservice.go` 定义，位图图标在 `statusicon.go` 定义。
- 字符宽度参考 `@evenrealities/pretext`；文本与图标使用独立区域，按容器可用像素截断，不能按等宽字体估算。
- 修改眼镜显示后运行 `go test ./...`；预览与真机验证结果应明确区分。
- Hook 会话通过本地状态与流文件保留；进入眼镜菜单后才推送浏览页面，退出后停止页面推送。Codex 新完成事件使用 SDK 原生通知，启动不补发历史通知。
- 右侧任务预览默认在主窗口 Canvas 内渲染，数据桥接在 `simulator.go`，必须复用实际发送的容器参数与 BMP；官方模拟器只能由用户点击按钮后手动启动。
